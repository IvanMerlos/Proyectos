/******************************************************************************
Escribir un algoritmo para calcular la distancia recorrida (m) por un m´ovil
 que se desplaza con velocidad constante (m/s) durante un tiempo (s). La
 velocidad y el tiempo ser´an ingresadas por el usuario
*******************************************************************************/
#include <stdio.h>
float veloc,tiemp,distancia;
int main()
{
    printf("Introduzca la velocidad(m/s): ");
    scanf("%f",&veloc);
    printf("Ingrese tiempo(s): ");
    scanf("%f",&tiemp);
    
   printf("La distancia en m es %.2f\n",distancia = veloc*tiemp);
    printf("La velocidad en m/s es %.2f\n",veloc = distancia/tiemp);
    printf("El tiempo en s es %.2f\n",tiemp= distancia/veloc);
    return 0;
}
